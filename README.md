Getting started
---------------

    vagrant up --no-provision
    vagrant ssh
    sudo apt-get update
    exit
    vagrant up

if you have networking problems:

    sudo /etc/init.d/networking restart
